---
import { Image } from 'astro:assets'

type Props = {
  src: ImageMetadata
  alt: string
}

const { src, alt } = Astro.props
---

<Image {src} {alt} width={50} height={50} loading="eager" class="aspect-square rounded-full bg-secondary-100 object-contain" />
